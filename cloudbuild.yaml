# Liste des Cloud Builders : https://console.cloud.google.com/gcr/images/cloud-builders/GLOBAL
steps:
  - name: "ghcr.io/astral-sh/uv:python3.12-trixie-slim"
    id: CI
    entrypoint: /bin/sh
    args:
      - "-c"
      - "chmod a+x install.sh && ./install.sh && uv sync && uv pip install pytest pytest-cov mlflow==3.0.1 && uv run pytest ."
    env:
      - "UV_CACHE_DIR=/root/.cache/uv"

logs_bucket: gs://ml_ops_final/logs_build_cloud_build
